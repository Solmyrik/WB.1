// creatingTemplate
// не совсем понял эту задачу

function creatingTemplate(element) {
  // создаем шаблон
  const template = document.createElement('template');

  // добавляем его в нужный элемент
  element.appendChild(template);
}
